/* Do not remove the headers from this file! see /USAGE for more info. */

inherit ARMOUR;

void setup()
{
   set_adj("black");
   set_id("helm");
   set_proper_name("A grunts Death's Head helm");
   set_long("The helmet looks like a black skull made of bonded ceramic and is harder then steel.");
   set_armour_class(4);
   //set_mdc(40);
   set_wearmsg("$N $vput on a $o.");
   set_slot("head");
   
}
