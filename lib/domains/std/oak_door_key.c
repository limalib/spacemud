/* Do not remove the headers from this file! see /USAGE for more info. */

inherit KEY;

void setup()
{
    set_adj("large", "metal");
    set_in_room_desc("A large metal key lies on the ground.");
    set_long("This seems to be a rather large key for a door perhaps?");
    set_key_type( "large_oak_door" );
}
